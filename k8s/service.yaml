apiVersion: v1
kind: Service
metadata:
  name: healthcare-devsecops-service
spec:
  type: NodePort
  selector:
    app: healthcare-devsecops
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30082